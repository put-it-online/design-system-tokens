import { AirDesignSystemTokens } from "../../types";
import { colors } from "./colors";
import { fontSizes } from "./fontSizes";

export const airDesignSystemTokens: AirDesignSystemTokens = {
    colors,
    fontSizes
};